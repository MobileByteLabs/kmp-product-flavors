name: Publish to Maven Central

on:
  release:
    types: [ published ]
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish (e.g., 1.0.0-alpha01)'
        required: true
        type: string

env:
  MAVEN_CENTRAL_USERNAME: ${{ secrets.MAVEN_CENTRAL_USERNAME }}
  MAVEN_CENTRAL_PASSWORD: ${{ secrets.MAVEN_CENTRAL_PASSWORD }}
  SIGNING_KEY_ID: ${{ secrets.SIGNING_KEY_ID }}
  SIGNING_PASSWORD: ${{ secrets.SIGNING_PASSWORD }}

jobs:
  publish:
    name: Publish Plugin
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Decode GPG Key
        run: |
          echo "${{ secrets.GPG_KEY_CONTENTS }}" | base64 -d > signing-key.gpg
          gpg --batch --import signing-key.gpg
          rm signing-key.gpg

      - name: Build Plugin
        run: ./gradlew :build-logic:flavor-plugin:build --no-daemon

      - name: Run Tests
        run: ./gradlew :build-logic:flavor-plugin:test --no-daemon

      - name: Publish to Maven Central
        run: |
          ./gradlew :build-logic:flavor-plugin:publishAllPublicationsToMavenCentral \
            --no-daemon \
            -Psigning.gnupg.keyName=${{ secrets.SIGNING_KEY_ID }} \
            -Psigning.gnupg.passphrase=${{ secrets.SIGNING_PASSWORD }}

      - name: Create GitHub Release Notes
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v1
        with:
          files: |
            build-logic/flavor-plugin/build/libs/*.jar
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  publish-gradle-plugin-portal:
    name: Publish to Gradle Plugin Portal
    runs-on: ubuntu-latest
    needs: publish

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Publish to Gradle Plugin Portal
        run: |
          ./gradlew :build-logic:flavor-plugin:publishPlugins \
            --no-daemon \
            -Pgradle.publish.key=${{ secrets.GRADLE_PUBLISH_KEY }} \
            -Pgradle.publish.secret=${{ secrets.GRADLE_PUBLISH_SECRET }}

  notify:
    name: Notify
    runs-on: ubuntu-latest
    needs: [ publish, publish-gradle-plugin-portal ]
    if: always()

    steps:
      - name: Check Status
        id: status
        run: |
          if [ "${{ needs.publish.result }}" == "success" ] && [ "${{ needs.publish-gradle-plugin-portal.result }}" == "success" ]; then
            echo "status=success" >> $GITHUB_OUTPUT
          else
            echo "status=failure" >> $GITHUB_OUTPUT
          fi

      - name: Create Summary
        run: |
          echo "## Release Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.status.outputs.status }}" == "success" ]; then
            echo "Published successfully to:" >> $GITHUB_STEP_SUMMARY
            echo "- Maven Central" >> $GITHUB_STEP_SUMMARY
            echo "- Gradle Plugin Portal" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "Installation:" >> $GITHUB_STEP_SUMMARY
            echo '```kotlin' >> $GITHUB_STEP_SUMMARY
            echo 'plugins {' >> $GITHUB_STEP_SUMMARY
            echo '    id("io.github.anthropic.kmp-product-flavors") version "${{ github.event.release.tag_name || inputs.version }}"' >> $GITHUB_STEP_SUMMARY
            echo '}' >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
          else
            echo "Release failed. Check the logs for details." >> $GITHUB_STEP_SUMMARY
          fi
