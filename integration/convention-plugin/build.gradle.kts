/*
 * Convention Plugin Integration for kmp-project-template
 *
 * This module provides ready-to-use convention plugin files that can be
 * copied into the kmp-project-template's build-logic/convention directory.
 *
 * These files are NOT compiled as part of this project - they are provided
 * as source templates to be integrated into the target project's build logic.
 */

// This is a documentation-only module - no actual build configuration needed
// The source files in src/main/kotlin/ are templates to be copied to kmp-project-template
